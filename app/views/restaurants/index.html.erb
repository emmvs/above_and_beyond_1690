<div class="container">
  <p style="color: green"><%= notice %></p>

  <h1>Restaurants</h1>

  <%= link_to "New restaurant", new_restaurant_path, class: 'btn btn-outline-secondary' %>
  <%= link_to "Show me the best!!! 👑", top_restaurants_path, class: 'btn btn-outline-primary' %>

  <div id="restaurants">
    <% @restaurants.each do |restaurant| %>
      <div class="card m-3 p-2">
        <%= link_to restaurant, class: 'btn' do %>
          <%= render restaurant %>
        <% end %>
        <p>
        </p>
      </div>
    <% end %>
  </div>
</div>
